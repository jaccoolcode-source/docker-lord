# Docker Desktop TCP endpoint (required — enable in Docker Desktop Settings > General)
# "Expose daemon on tcp://localhost:2375 without TLS" must be checked
DOCKER_TCP_HOST=host.docker.internal
DOCKER_TCP_PORT=2375
